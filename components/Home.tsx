import React from 'react';
import HeroTitle from './HeroTitle';
import Cards from './Cards';

const Home = () => {
    return (
        <div>
            <HeroTitle />
            <Cards />
        </div>
    );
};

export default Home;
